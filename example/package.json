{
  "name": "example",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "mana build && vite",
    "build": "mana build && vite build",
    "start": "vite preview"
  },
  "dependencies": {
    "mana-engine": "workspace:*"
  },
  "devDependencies": {
    "vite": "^6.3.0"
  }
}
