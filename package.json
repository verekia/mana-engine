{
  "name": "mana-engine-monorepo",
  "private": true,
  "workspaces": [
    "library",
    "example"
  ],
  "type": "module",
  "scripts": {
    "lint": "oxlint --deny-warnings",
    "format": "oxfmt --write .",
    "format:check": "oxfmt --check .",
    "typecheck": "tsgo --noEmit",
    "test": "bun test",
    "all": "bun run lint && bun run format:check && bun run typecheck && bun run test && bun run build",
    "dev": "bun run --filter 'example' dev",
    "build": "bun run --filter 'mana-engine' build && bun run --filter 'example' build",
    "start": "bun run --filter 'example' start"
  },
  "devDependencies": {
    "@typescript/native-preview": "7.0.0-dev.20260322.1",
    "oxfmt": "0.41.0",
    "oxlint": "1.56.0"
  }
}
